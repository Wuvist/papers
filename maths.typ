$ U^(*)(x, y, z ) >= z + root(, z) - 1 / z $

Let $a$, $b$, and $c$ be the side lengths of right-angled triangle. Then, we know that:

$ a^2 + b^2 = c^2 $

Prove by induction:
$ sum_(k=1)^n k = (n(n+1)) / 2 $

$ sum_(i=0)^n a_i = 2^(1+i) + x_1 $

$ mat(
    delim: "{",
  1, 2, ..., 10;
  2, 2, ..., 10;
  dots.v, dots.v, dots.down, dots.v;
  10, 10, ..., 10;
) + z + root(, z) - 1 / z => sum_(i=0)^n a_i = 2^(1+i) + x_1 $
